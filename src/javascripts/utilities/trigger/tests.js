define([
  'jquery',
  'underscore',
  'backbone',
  './utility'
], function($, _, Backbone, trigger) {
  describe('Trigger Utility', function() {
    describe('Utility', function() {
      it('Exists', function() {
        assert.ok(trigger);
      });
    });
  });
});